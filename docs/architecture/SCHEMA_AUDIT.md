# Agnexis Schema Audit

## Purpose

Read-only inventory of every Drift column against the four-layer rule: persistence should hold real-world captured facts, not derived summaries used as system truth.

## Date

2026-04-27

## Architecture Rule Summary

- **Layer 1 — Fact:** What happened or what was observed/entered/imported at a point in time, across three stored categories: RAW FACT, FACT SNAPSHOT, ROUND-TRIP STORAGE.
- **Layers 2–4:** Computed values, composed relationships, and labels belong in providers/UI unless they are explicit **round-trip** artifacts for external systems.
- **Labels in this document:**
  - **RAW FACT** — real-world event or attribute captured directly
  - **FACT SNAPSHOT** — time-bound evaluation recorded at a specific event, never recomputed after (e.g., exportConfidence, findingsJson, hasSparseData)
  - **DERIVED STATE** — computable from other stored facts; must not be stored except under explicit exception in TRIAL_MODEL.md
  - **ROUND-TRIP STORAGE** — verbatim external data preserved for compatibility, not used as internal truth
  - **UNCLEAR** — needs product decision before classification can be confirmed

**Source:** All Drift tables are defined in `lib/core/database/app_database.dart` only. **Counts at last audit:** 43 tables, 779 columns. These counts may become stale after future schema additions; re-run the audit after SETypeProfiles is activated and after any subsequent table additions.

## Full Column Inventory

### `Users`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Users` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| displayName | Text | `Users` column `displayName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| initials | Text | `Users` column `initials` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| roleKey | Text | `Users` column `roleKey` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isActive | Bool | `Users` column `isActive` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| pinHash | Text | `Users` column `pinHash` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| pinEnabled | Bool | `Users` column `pinEnabled` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `Users` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedAt | DateTime | `Users` column `updatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Trials`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Trials` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| name | Text | `Trials` column `name` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| crop | Text | `Trials` column `crop` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| location | Text | `Trials` column `location` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| season | Text | `Trials` column `season` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `Trials` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotDimensions | Text | Plot dimensions (e.g. "10 m × 2 m"). Trial-level default. (`plotDimensions`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotRows | Int | Number of rows per plot. Trial-level default. (`plotRows`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotSpacing | Text | Spacing between plots (e.g. "0.5 m"). Trial-level default. (`plotSpacing`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sponsor | Text | `Trials` column `sponsor` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| protocolNumber | Text | `Trials` column `protocolNumber` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| investigatorName | Text | `Trials` column `investigatorName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cooperatorName | Text | `Trials` column `cooperatorName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| siteId | Text | `Trials` column `siteId` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldName | Text | `Trials` column `fieldName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| county | Text | `Trials` column `county` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| stateProvince | Text | `Trials` column `stateProvince` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| country | Text | `Trials` column `country` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| latitude | Real | `Trials` column `latitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| longitude | Real | `Trials` column `longitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| elevationM | Real | `Trials` column `elevationM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| experimentalDesign | Text | `Trials` column `experimentalDesign` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotLengthM | Real | `Trials` column `plotLengthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotWidthM | Real | `Trials` column `plotWidthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| alleyLengthM | Real | `Trials` column `alleyLengthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| previousCrop | Text | `Trials` column `previousCrop` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| tillage | Text | `Trials` column `tillage` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| irrigated | Bool | `Trials` column `irrigated` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilSeries | Text | `Trials` column `soilSeries` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilTexture | Text | `Trials` column `soilTexture` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| organicMatterPct | Real | `Trials` column `organicMatterPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilPh | Real | `Trials` column `soilPh` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestDate | DateTime | `Trials` column `harvestDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| studyType | Text | `Trials` column `studyType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| workspaceType | Text | Workspace type: variety \| efficacy \| glp \| standalone (`workspaceType`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `Trials` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedAt | DateTime | `Trials` column `updatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `Trials` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `Trials` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `Trials` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| siteRefId | Int | FK to shared site record. Nullable — trials without a linked site still store location/soil fields directly (denormalized copies). (`siteRefId`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cultivar | Text | Cultivar / variety name (distinct from [crop] which is species). (`cultivar`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rowSpacingCm | Real | Row spacing in cm. Combined with [plantSpacingCm] gives crop density. (`rowSpacingCm`, Real). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plantSpacingCm | Real | Plant spacing within row, in cm. (`plantSpacingCm`, Real). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| gepComplianceFlag | Bool | Whether the trial follows Good Experimental Practice guidelines. (`gepComplianceFlag`, Bool). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Treatments`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Treatments` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Treatments` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| code | Text | `Treatments` column `code` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| name | Text | `Treatments` column `name` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| description | Text | `Treatments` column `description` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatmentType | Text | `Treatments` column `treatmentType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingCode | Text | `Treatments` column `timingCode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| eppoCode | Text | `Treatments` column `eppoCode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `Treatments` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `Treatments` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `Treatments` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedByUserId | Int | `Treatments` column `lastEditedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedAt | DateTime | `Treatments` column `lastEditedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `TreatmentComponents`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `TreatmentComponents` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatmentId | Int | `TreatmentComponents` column `treatmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `TreatmentComponents` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| productName | Text | `TreatmentComponents` column `productName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rate | Real | `TreatmentComponents` column `rate` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rateUnit | Text | `TreatmentComponents` column `rateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationTiming | Text | `TreatmentComponents` column `applicationTiming` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `TreatmentComponents` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | `TreatmentComponents` column `sortOrder` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| activeIngredientPct | Real | `TreatmentComponents` column `activeIngredientPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| formulationType | Text | `TreatmentComponents` column `formulationType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| manufacturer | Text | `TreatmentComponents` column `manufacturer` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| registrationNumber | Text | `TreatmentComponents` column `registrationNumber` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| eppoCode | Text | `TreatmentComponents` column `eppoCode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| activeIngredientName | Text | Active ingredient common name (e.g. "glyphosate"). (`activeIngredientName`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| aiConcentration | Real | AI concentration (g/L or g/kg). Paired with [aiConcentrationUnit]. (`aiConcentration`, Real). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| aiConcentrationUnit | Text | `TreatmentComponents` column `aiConcentrationUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| labelRate | Real | Maximum legal rate from the product label, separate from trial rate. (`labelRate`, Real). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| labelRateUnit | Text | `TreatmentComponents` column `labelRateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isTestProduct | Bool | Distinguishes test product from reference standard. (`isTestProduct`, Bool). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `TreatmentComponents` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `TreatmentComponents` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `TreatmentComponents` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedByUserId | Int | `TreatmentComponents` column `lastEditedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedAt | DateTime | `TreatmentComponents` column `lastEditedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Assessments`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Assessments` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Assessments` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| name | Text | `Assessments` column `name` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| dataType | Text | `Assessments` column `dataType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| minValue | Real | `Assessments` column `minValue` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| maxValue | Real | `Assessments` column `maxValue` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| unit | Text | `Assessments` column `unit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isActive | Bool | `Assessments` column `isActive` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `AssessmentDefinitions`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `AssessmentDefinitions` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| code | Text | `AssessmentDefinitions` column `code` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| name | Text | `AssessmentDefinitions` column `name` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| category | Text | `AssessmentDefinitions` column `category` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| dataType | Text | `AssessmentDefinitions` column `dataType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| unit | Text | `AssessmentDefinitions` column `unit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| scaleMin | Real | `AssessmentDefinitions` column `scaleMin` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| scaleMax | Real | `AssessmentDefinitions` column `scaleMax` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| target | Text | `AssessmentDefinitions` column `target` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| method | Text | `AssessmentDefinitions` column `method` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| defaultInstructions | Text | `AssessmentDefinitions` column `defaultInstructions` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingType | Text | `AssessmentDefinitions` column `timingType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isSystem | Bool | `AssessmentDefinitions` column `isSystem` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isActive | Bool | `AssessmentDefinitions` column `isActive` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `AssessmentDefinitions` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedAt | DateTime | `AssessmentDefinitions` column `updatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingCode | Text | Rating date string from CSV header (e.g. "1-Jul-26"). Misnamed — stores date, not timing code. Technical debt: do not rename in this sprint. (`timingCode`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| daysAfterTreatment | Int | `AssessmentDefinitions` column `daysAfterTreatment` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assessmentMethod | Text | `AssessmentDefinitions` column `assessmentMethod` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| validMin | Real | `AssessmentDefinitions` column `validMin` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| validMax | Real | `AssessmentDefinitions` column `validMax` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| eppoCode | Text | `AssessmentDefinitions` column `eppoCode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cropPart | Text | Plant part assessed (PLANT, LEAF3, etc.) — populated from shell Part Rated. (`cropPart`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingDescription | Text | `AssessmentDefinitions` column `timingDescription` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| resultDirection | Text | `AssessmentDefinitions` column `resultDirection` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| appTimingCode | Text | Application timing code (A1, A3, A6, A9, AA) from shell row 42 (0-based). (`appTimingCode`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trtEvalInterval | Text | Treatment-evaluation interval (e.g. "-28 DA-A") from shell row 42 (0-based). (`trtEvalInterval`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| collectBasis | Text | Collect basis (PLOT, etc.) from shell row 23 (0-based). (`collectBasis`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `TrialAssessments`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `TrialAssessments` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `TrialAssessments` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assessmentDefinitionId | Int | `TrialAssessments` column `assessmentDefinitionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| displayNameOverride | Text | `TrialAssessments` column `displayNameOverride` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| required | Bool | `TrialAssessments` column `required` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| selectedFromProtocol | Bool | `TrialAssessments` column `selectedFromProtocol` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| selectedManually | Bool | `TrialAssessments` column `selectedManually` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| defaultInSessions | Bool | `TrialAssessments` column `defaultInSessions` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | `TrialAssessments` column `sortOrder` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingMode | Text | `TrialAssessments` column `timingMode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| daysAfterPlanting | Int | `TrialAssessments` column `daysAfterPlanting` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| daysAfterTreatment | Int | `TrialAssessments` column `daysAfterTreatment` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| growthStage | Text | `TrialAssessments` column `growthStage` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| methodOverride | Text | `TrialAssessments` column `methodOverride` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| instructionOverride | Text | `TrialAssessments` column `instructionOverride` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isActive | Bool | `TrialAssessments` column `isActive` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| legacyAssessmentId | Int | `TrialAssessments` column `legacyAssessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `TrialAssessments` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedAt | DateTime | `TrialAssessments` column `updatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| pestName | Text | `TrialAssessments` column `pestName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| eppoCodeLocal | Text | `TrialAssessments` column `eppoCodeLocal` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| bbchScale | Text | `TrialAssessments` column `bbchScale` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cropStageAtAssessment | Text | `TrialAssessments` column `cropStageAtAssessment` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Plots`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Plots` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Plots` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotId | Text | `Plots` column `plotId` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotSortIndex | Int | `Plots` column `plotSortIndex` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rep | Int | `Plots` column `rep` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatmentId | Int | `Plots` column `treatmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| row | Text | `Plots` column `row` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| column | Text | `Plots` column `column` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldRow | Int | `Plots` column `fieldRow` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldColumn | Int | `Plots` column `fieldColumn` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assignmentSource | Text | Assignment provenance: 'imported' \| 'manual' \| null (unknown). (`assignmentSource`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assignmentUpdatedAt | DateTime | `Plots` column `assignmentUpdatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotLengthM | Real | `Plots` column `plotLengthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotWidthM | Real | `Plots` column `plotWidthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotAreaM2 | Real | `Plots` column `plotAreaM2` (Real); no adjacent `///` doc in schema file. | UNCLEAR | May be measured, imported, or computed from dimensions. | Medium | Plots tab, layout |
| harvestLengthM | Real | `Plots` column `harvestLengthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestWidthM | Real | `Plots` column `harvestWidthM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestAreaM2 | Real | `Plots` column `harvestAreaM2` (Real); no adjacent `///` doc in schema file. | UNCLEAR | Same ambiguity as harvested dimensions. | Medium | Harvest / yield |
| plotDirection | Text | `Plots` column `plotDirection` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilSeries | Text | `Plots` column `soilSeries` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotNotes | Text | `Plots` column `plotNotes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isGuardRow | Bool | Field layout: non-data / border plot. v2: excluded from rating queue by default; display + editing unchanged in Plots tab. (`isGuardRow`, Bool). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `Plots` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `Plots` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `Plots` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| excludeFromAnalysis | Bool | `Plots` column `excludeFromAnalysis` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| exclusionReason | Text | `Plots` column `exclusionReason` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| damageType | Text | `Plots` column `damageType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| armPlotNumber | Int | Canonical ARM plot number (matches Rating Shell plot column); nullable for non-ARM plots. (`armPlotNumber`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| armImportDataRowIndex | Int | Index into source ARM CSV data rows for this plot row (import alignment). (`armImportDataRowIndex`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Assignments`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Assignments` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Assignments` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotId | Int | `Assignments` column `plotId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatmentId | Int | `Assignments` column `treatmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| replication | Int | `Assignments` column `replication` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| block | Int | `Assignments` column `block` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| range | Int | `Assignments` column `range` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| column | Int | `Assignments` column `column` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| position | Int | `Assignments` column `position` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isCheck | Bool | `Assignments` column `isCheck` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isControl | Bool | `Assignments` column `isControl` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assignmentSource | Text | `Assignments` column `assignmentSource` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assignedAt | DateTime | `Assignments` column `assignedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assignedBy | Int | `Assignments` column `assignedBy` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `Assignments` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `Assignments` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedAt | DateTime | `Assignments` column `updatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Sessions`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Sessions` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Sessions` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| name | Text | `Sessions` column `name` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| startedAt | DateTime | `Sessions` column `startedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| endedAt | DateTime | `Sessions` column `endedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionDateLocal | Text | `Sessions` column `sessionDateLocal` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| raterName | Text | `Sessions` column `raterName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdByUserId | Int | `Sessions` column `createdByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `Sessions` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `Sessions` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `Sessions` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `Sessions` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cropStageBbch | Int | Optional BBCH growth stage (0–99) at rating session; null if not recorded. (`cropStageBbch`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cropInjuryStatus | Text | Crop injury / phytotoxicity status recorded at session close. Values: 'none_observed', 'symptoms_observed', 'not_assessed', or null (not yet recorded). (`cropInjuryStatus`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cropInjuryNotes | Text | Free-text description when cropInjuryStatus = 'symptoms_observed'. (`cropInjuryNotes`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cropInjuryPhotoIds | Text | JSON array of photo IDs attached to the crop injury observation. (`cropInjuryPhotoIds`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `SessionAssessments`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `SessionAssessments` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `SessionAssessments` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assessmentId | Int | `SessionAssessments` column `assessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialAssessmentId | Int | `SessionAssessments` column `trialAssessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | User-defined order for rating flow (0, 1, 2, …). Same sequence applies to every plot. (`sortOrder`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `RatingRecords`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `RatingRecords` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `RatingRecords` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `RatingRecords` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assessmentId | Int | `RatingRecords` column `assessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialAssessmentId | Int | `RatingRecords` column `trialAssessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `RatingRecords` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| subUnitId | Int | `RatingRecords` column `subUnitId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| resultStatus | Text | `RatingRecords` column `resultStatus` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| numericValue | Real | `RatingRecords` column `numericValue` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| textValue | Text | `RatingRecords` column `textValue` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isCurrent | Bool | `RatingRecords` column `isCurrent` (Bool); no adjacent `///` doc in schema file. | DERIVED STATE | Denormalized “current revision” flag; derivable from the amendment chain. Accepted maintained-index exception — written atomically by a single write path. See Known Exceptions in TRIAL_MODEL.md. | High | Rating flow, history views, export |
| previousId | Int | `RatingRecords` column `previousId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `RatingRecords` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| raterName | Text | `RatingRecords` column `raterName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAppVersion | Text | `RatingRecords` column `createdAppVersion` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdDeviceInfo | Text | `RatingRecords` column `createdDeviceInfo` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| capturedLatitude | Real | `RatingRecords` column `capturedLatitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| capturedLongitude | Real | `RatingRecords` column `capturedLongitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| ratingTime | Text | Local time of rating as HH:mm. (`ratingTime`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| ratingMethod | Text | `RatingRecords` column `ratingMethod` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| confidence | Text | certain \| uncertain \| estimated (`confidence`, Text). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| amended | Bool | `RatingRecords` column `amended` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| originalValue | Text | `RatingRecords` column `originalValue` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| amendmentReason | Text | `RatingRecords` column `amendmentReason` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| amendedBy | Text | `RatingRecords` column `amendedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| amendedAt | DateTime | `RatingRecords` column `amendedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedByUserId | Int | `RatingRecords` column `lastEditedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedAt | DateTime | `RatingRecords` column `lastEditedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `RatingRecords` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `RatingRecords` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `RatingRecords` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `RatingCorrections`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `RatingCorrections` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| ratingId | Int | `RatingCorrections` column `ratingId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| oldNumericValue | Real | `RatingCorrections` column `oldNumericValue` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| newNumericValue | Real | `RatingCorrections` column `newNumericValue` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| oldTextValue | Text | `RatingCorrections` column `oldTextValue` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| newTextValue | Text | `RatingCorrections` column `newTextValue` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| oldResultStatus | Text | `RatingCorrections` column `oldResultStatus` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| newResultStatus | Text | `RatingCorrections` column `newResultStatus` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| reason | Text | `RatingCorrections` column `reason` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| correctedByUserId | Int | `RatingCorrections` column `correctedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| correctedAt | DateTime | `RatingCorrections` column `correctedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `RatingCorrections` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `RatingCorrections` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Notes`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Notes` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Notes` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `Notes` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `Notes` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| content | Text | `Notes` column `content` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `Notes` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| raterName | Text | `Notes` column `raterName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedAt | DateTime | `Notes` column `updatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| updatedBy | Text | `Notes` column `updatedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `Notes` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `Notes` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `Notes` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Photos`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Photos` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `Photos` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `Photos` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `Photos` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| filePath | Text | `Photos` column `filePath` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| tempPath | Text | `Photos` column `tempPath` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `Photos` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| caption | Text | `Photos` column `caption` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `Photos` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assessmentId | Int | Assessment linked to this photo (photo-anchored rating). (`assessmentId`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| ratingValue | Real | Rating value at the time the photo was captured. (`ratingValue`, Real). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isDeleted | Bool | `Photos` column `isDeleted` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedAt | DateTime | `Photos` column `deletedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deletedBy | Text | `Photos` column `deletedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `PlotFlags`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `PlotFlags` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `PlotFlags` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `PlotFlags` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `PlotFlags` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| flagType | Text | `PlotFlags` column `flagType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| description | Text | `PlotFlags` column `description` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `PlotFlags` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| raterName | Text | `PlotFlags` column `raterName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `DeviationFlags`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `DeviationFlags` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `DeviationFlags` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `DeviationFlags` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `DeviationFlags` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| ratingRecordId | Int | `DeviationFlags` column `ratingRecordId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deviationType | Text | `DeviationFlags` column `deviationType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| description | Text | `DeviationFlags` column `description` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `DeviationFlags` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| raterName | Text | `DeviationFlags` column `raterName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `SeedingRecords`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `SeedingRecords` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `SeedingRecords` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `SeedingRecords` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `SeedingRecords` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedingDate | DateTime | `SeedingRecords` column `seedingDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| operatorName | Text | `SeedingRecords` column `operatorName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| comments | Text | `SeedingRecords` column `comments` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `SeedingRecords` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ProtocolSeedingFields`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ProtocolSeedingFields` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `ProtocolSeedingFields` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldKey | Text | `ProtocolSeedingFields` column `fieldKey` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldLabel | Text | `ProtocolSeedingFields` column `fieldLabel` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldType | Text | `ProtocolSeedingFields` column `fieldType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| unit | Text | `ProtocolSeedingFields` column `unit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isRequired | Bool | `ProtocolSeedingFields` column `isRequired` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| isVisible | Bool | `ProtocolSeedingFields` column `isVisible` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | `ProtocolSeedingFields` column `sortOrder` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| source | Text | `ProtocolSeedingFields` column `source` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `SeedingFieldValues`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `SeedingFieldValues` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedingRecordId | Int | `SeedingFieldValues` column `seedingRecordId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldKey | Text | `SeedingFieldValues` column `fieldKey` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldLabel | Text | `SeedingFieldValues` column `fieldLabel` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| valueText | Text | `SeedingFieldValues` column `valueText` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| valueNumber | Real | `SeedingFieldValues` column `valueNumber` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| valueDate | Text | `SeedingFieldValues` column `valueDate` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| valueBool | Bool | `SeedingFieldValues` column `valueBool` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| unit | Text | `SeedingFieldValues` column `unit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | `SeedingFieldValues` column `sortOrder` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ApplicationSlots`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ApplicationSlots` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `ApplicationSlots` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| slotCode | Text | `ApplicationSlots` column `slotCode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingLabel | Text | `ApplicationSlots` column `timingLabel` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| methodDefault | Text | `ApplicationSlots` column `methodDefault` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plannedGrowthStage | Text | `ApplicationSlots` column `plannedGrowthStage` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| protocolNotes | Text | `ApplicationSlots` column `protocolNotes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | `ApplicationSlots` column `sortOrder` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `ApplicationSlots` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ApplicationEvents`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ApplicationEvents` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `ApplicationEvents` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `ApplicationEvents` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationSlotId | Int | `ApplicationEvents` column `applicationSlotId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationNumber | Int | `ApplicationEvents` column `applicationNumber` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| timingLabel | Text | `ApplicationEvents` column `timingLabel` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| method | Text | `ApplicationEvents` column `method` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `ApplicationEvents` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationDate | DateTime | `ApplicationEvents` column `applicationDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| growthStage | Text | `ApplicationEvents` column `growthStage` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| operatorName | Text | `ApplicationEvents` column `operatorName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| equipment | Text | `ApplicationEvents` column `equipment` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| weather | Text | `ApplicationEvents` column `weather` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `ApplicationEvents` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| partialFlag | Bool | `ApplicationEvents` column `partialFlag` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| completedAt | DateTime | `ApplicationEvents` column `completedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| completedBy | Text | `ApplicationEvents` column `completedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `ApplicationEvents` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ApplicationPlotRecords`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ApplicationPlotRecords` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| eventId | Int | `ApplicationPlotRecords` column `eventId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `ApplicationPlotRecords` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `ApplicationPlotRecords` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `ApplicationPlotRecords` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `ApplicationPlotRecords` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `ApplicationPlotRecords` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `AuditEvents`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `AuditEvents` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `AuditEvents` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionId | Int | `AuditEvents` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotPk | Int | `AuditEvents` column `plotPk` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| eventType | Text | `AuditEvents` column `eventType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| description | Text | `AuditEvents` column `description` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| performedBy | Text | `AuditEvents` column `performedBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| performedByUserId | Int | `AuditEvents` column `performedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `AuditEvents` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| metadata | Text | `AuditEvents` column `metadata` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ImportEvents`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ImportEvents` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `ImportEvents` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fileName | Text | `ImportEvents` column `fileName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| savedFilePath | Text | `ImportEvents` column `savedFilePath` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `ImportEvents` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rowsImported | Int | `ImportEvents` column `rowsImported` (Int); no adjacent `///` doc in schema file. | DERIVED STATE | Aggregate count emitted by importer, not a single-field observation. | Medium | Import history, diagnostics |
| rowsSkipped | Int | `ImportEvents` column `rowsSkipped` (Int); no adjacent `///` doc in schema file. | DERIVED STATE | Aggregate count from import processing. | Medium | Import history, diagnostics |
| warnings | Text | `ImportEvents` column `warnings` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `ImportEvents` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `SeedingEvents`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Text | `SeedingEvents` column `id` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `SeedingEvents` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedingDate | DateTime | `SeedingEvents` column `seedingDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| operatorName | Text | `SeedingEvents` column `operatorName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedLotNumber | Text | `SeedingEvents` column `seedLotNumber` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedingRate | Real | `SeedingEvents` column `seedingRate` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedingRateUnit | Text | `SeedingEvents` column `seedingRateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedingDepth | Real | `SeedingEvents` column `seedingDepth` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rowSpacing | Real | `SeedingEvents` column `rowSpacing` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| equipmentUsed | Text | `SeedingEvents` column `equipmentUsed` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `SeedingEvents` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| variety | Text | `SeedingEvents` column `variety` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedTreatment | Text | `SeedingEvents` column `seedTreatment` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| germinationPct | Real | `SeedingEvents` column `germinationPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| emergenceDate | DateTime | `SeedingEvents` column `emergenceDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| emergencePct | Real | `SeedingEvents` column `emergencePct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plantingMethod | Text | `SeedingEvents` column `plantingMethod` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| status | Text | `SeedingEvents` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| completedAt | DateTime | `SeedingEvents` column `completedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `SeedingEvents` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedByUserId | Int | `SeedingEvents` column `lastEditedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedAt | DateTime | `SeedingEvents` column `lastEditedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| temperatureC | Real | `SeedingEvents` column `temperatureC` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| humidityPct | Real | `SeedingEvents` column `humidityPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windSpeedKmh | Real | `SeedingEvents` column `windSpeedKmh` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windDirection | Text | `SeedingEvents` column `windDirection` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cloudCoverPct | Real | `SeedingEvents` column `cloudCoverPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| precipitation | Text | `SeedingEvents` column `precipitation` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| precipitationMm | Real | `SeedingEvents` column `precipitationMm` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilMoisture | Text | `SeedingEvents` column `soilMoisture` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilTemperature | Real | `SeedingEvents` column `soilTemperature` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| conditionsRecordedAt | DateTime | `SeedingEvents` column `conditionsRecordedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| capturedLatitude | Real | `SeedingEvents` column `capturedLatitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| capturedLongitude | Real | `SeedingEvents` column `capturedLongitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| locationCapturedAt | DateTime | `SeedingEvents` column `locationCapturedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `TrialApplicationEvents`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Text | `TrialApplicationEvents` column `id` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `TrialApplicationEvents` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatmentId | Int | `TrialApplicationEvents` column `treatmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationDate | DateTime | `TrialApplicationEvents` column `applicationDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| growthStageCode | Text | `TrialApplicationEvents` column `growthStageCode` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| operatorName | Text | `TrialApplicationEvents` column `operatorName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| equipmentUsed | Text | `TrialApplicationEvents` column `equipmentUsed` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| productName | Text | `TrialApplicationEvents` column `productName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rate | Real | `TrialApplicationEvents` column `rate` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rateUnit | Text | `TrialApplicationEvents` column `rateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| waterVolume | Real | `TrialApplicationEvents` column `waterVolume` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windSpeed | Real | `TrialApplicationEvents` column `windSpeed` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windDirection | Text | `TrialApplicationEvents` column `windDirection` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| temperature | Real | `TrialApplicationEvents` column `temperature` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| humidity | Real | `TrialApplicationEvents` column `humidity` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `TrialApplicationEvents` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationTime | Text | `TrialApplicationEvents` column `applicationTime` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationMethod | Text | `TrialApplicationEvents` column `applicationMethod` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| nozzleType | Text | `TrialApplicationEvents` column `nozzleType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| nozzleSpacingCm | Real | `TrialApplicationEvents` column `nozzleSpacingCm` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| operatingPressure | Real | `TrialApplicationEvents` column `operatingPressure` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| pressureUnit | Text | `TrialApplicationEvents` column `pressureUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| groundSpeed | Real | `TrialApplicationEvents` column `groundSpeed` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| speedUnit | Text | `TrialApplicationEvents` column `speedUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| adjuvantName | Text | `TrialApplicationEvents` column `adjuvantName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| adjuvantRate | Real | `TrialApplicationEvents` column `adjuvantRate` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| adjuvantRateUnit | Text | `TrialApplicationEvents` column `adjuvantRateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| spraySolutionPh | Real | `TrialApplicationEvents` column `spraySolutionPh` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| waterVolumeUnit | Text | `TrialApplicationEvents` column `waterVolumeUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cloudCoverPct | Real | `TrialApplicationEvents` column `cloudCoverPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilMoisture | Text | `TrialApplicationEvents` column `soilMoisture` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilTemperature | Real | `TrialApplicationEvents` column `soilTemperature` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilTempUnit | Text | `TrialApplicationEvents` column `soilTempUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilDepth | Real | `TrialApplicationEvents` column `soilDepth` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilDepthUnit | Text | `TrialApplicationEvents` column `soilDepthUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatedArea | Real | `TrialApplicationEvents` column `treatedArea` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| treatedAreaUnit | Text | `TrialApplicationEvents` column `treatedAreaUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotsTreated | Text | `TrialApplicationEvents` column `plotsTreated` (Text); no adjacent `///` doc in schema file. | DERIVED STATE | Transitional TEXT cache; ApplicationPlotAssignments is the canonical structured replacement. Dual-write in place since v55. Migration target — switch reads, remove writes, drop column. | High | Applications tab, plot overlays, export |
| status | Text | `TrialApplicationEvents` column `status` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| appliedAt | DateTime | `TrialApplicationEvents` column `appliedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `TrialApplicationEvents` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedByUserId | Int | `TrialApplicationEvents` column `lastEditedByUserId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastEditedAt | DateTime | `TrialApplicationEvents` column `lastEditedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| precipitation | Text | `TrialApplicationEvents` column `precipitation` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| precipitationMm | Real | `TrialApplicationEvents` column `precipitationMm` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| conditionsRecordedAt | DateTime | `TrialApplicationEvents` column `conditionsRecordedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| boomHeightCm | Real | `TrialApplicationEvents` column `boomHeightCm` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sessionName | Text | `TrialApplicationEvents` column `sessionName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| startedAt | DateTime | `TrialApplicationEvents` column `startedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| completedAt | DateTime | `TrialApplicationEvents` column `completedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| closedAt | DateTime | `TrialApplicationEvents` column `closedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| totalProductMixed | Real | Total product mixed in the tank (g, mL, or other unit matching rate). (`totalProductMixed`, Real). | UNCLEAR | Could be measured tank total or derived from mix math. | Medium | Application completion |
| totalAreaSprayedHa | Real | Total area actually sprayed (hectares). (`totalAreaSprayedHa`, Real). | UNCLEAR | Could be reported area or derived from plots. | Medium | Application completion |
| capturedLatitude | Real | `TrialApplicationEvents` column `capturedLatitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| capturedLongitude | Real | `TrialApplicationEvents` column `capturedLongitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| locationCapturedAt | DateTime | `TrialApplicationEvents` column `locationCapturedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `TrialApplicationProducts`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `TrialApplicationProducts` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialApplicationEventId | Text | `TrialApplicationProducts` column `trialApplicationEventId` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| productName | Text | `TrialApplicationProducts` column `productName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rate | Real | `TrialApplicationProducts` column `rate` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rateUnit | Text | `TrialApplicationProducts` column `rateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sortOrder | Int | `TrialApplicationProducts` column `sortOrder` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plannedProduct | Text | `TrialApplicationProducts` column `plannedProduct` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plannedRate | Real | `TrialApplicationProducts` column `plannedRate` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plannedRateUnit | Text | `TrialApplicationProducts` column `plannedRateUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| deviationFlag | Bool | `TrialApplicationProducts` column `deviationFlag` (Bool); no adjacent `///` doc in schema file. | UNCLEAR | Could be auto-set from plan vs actual or user toggled. | Medium | Application products UI |
| deviationNotes | Text | `TrialApplicationProducts` column `deviationNotes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ApplicationPlotAssignments`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ApplicationPlotAssignments` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicationEventId | Text | `ApplicationPlotAssignments` column `applicationEventId` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotLabel | Text | `ApplicationPlotAssignments` column `plotLabel` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotId | Int | `ApplicationPlotAssignments` column `plotId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ImportSnapshots`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ImportSnapshots` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `ImportSnapshots` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sourceFile | Text | `ImportSnapshots` column `sourceFile` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sourceRoute | Text | `ImportSnapshots` column `sourceRoute` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| armVersion | Text | `ImportSnapshots` column `armVersion` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| rawHeaders | Text | `ImportSnapshots` column `rawHeaders` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| columnOrder | Text | `ImportSnapshots` column `columnOrder` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| rowTypePatterns | Text | `ImportSnapshots` column `rowTypePatterns` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| plotCount | Int | `ImportSnapshots` column `plotCount` (Int); no adjacent `///` doc in schema file. | DERIVED STATE | Structural count from parser over source file. | Low | ARM import, compatibility |
| treatmentCount | Int | `ImportSnapshots` column `treatmentCount` (Int); no adjacent `///` doc in schema file. | DERIVED STATE | Structural count from parser. | Low | ARM import, compatibility |
| assessmentCount | Int | `ImportSnapshots` column `assessmentCount` (Int); no adjacent `///` doc in schema file. | DERIVED STATE | Structural count from parser. | Low | ARM import, compatibility |
| identityColumns | Text | `ImportSnapshots` column `identityColumns` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| assessmentTokens | Text | `ImportSnapshots` column `assessmentTokens` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| treatmentTokens | Text | `ImportSnapshots` column `treatmentTokens` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| plotTokens | Text | `ImportSnapshots` column `plotTokens` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| unknownPatterns | Text | `ImportSnapshots` column `unknownPatterns` (Text); no adjacent `///` doc in schema file. | DERIVED STATE | Parser classification of unrecognized patterns. | Low | Import diagnostics |
| hasSubsamples | Bool | `ImportSnapshots` column `hasSubsamples` (Bool); no adjacent `///` doc in schema file. | DERIVED STATE | Inferred boolean from file structure. | Low | Import routing |
| hasMultiApplication | Bool | `ImportSnapshots` column `hasMultiApplication` (Bool); no adjacent `///` doc in schema file. | DERIVED STATE | Inferred boolean from file structure. | Low | Import routing |
| hasSparseData | Bool | `ImportSnapshots` column `hasSparseData` (Bool); no adjacent `///` doc in schema file. | FACT SNAPSHOT | Written once at import from CSV cell data discarded after parsing. Cannot be recomputed without original CSV. Currently write-only from DB — active read path is the in-memory ParsedArmCsv during import. Forward-looking provenance. | Low | Import routing |
| hasRepeatedCodes | Bool | `ImportSnapshots` column `hasRepeatedCodes` (Bool); no adjacent `///` doc in schema file. | DERIVED STATE | Inferred boolean from file structure. | Low | Import routing |
| rawFileChecksum | Text | `ImportSnapshots` column `rawFileChecksum` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Captured file tokens/checksum for reproducible import and export routing. | Medium | ARM import, compatibility builder |
| capturedAt | DateTime | `ImportSnapshots` column `capturedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `CompatibilityProfiles`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `CompatibilityProfiles` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `CompatibilityProfiles` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| snapshotId | Int | `CompatibilityProfiles` column `snapshotId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| exportRoute | Text | `CompatibilityProfiles` column `exportRoute` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| columnMap | Text | `CompatibilityProfiles` column `columnMap` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| plotMap | Text | `CompatibilityProfiles` column `plotMap` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| treatmentMap | Text | `CompatibilityProfiles` column `treatmentMap` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| dataStartRow | Int | `CompatibilityProfiles` column `dataStartRow` (Int); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| headerEndRow | Int | `CompatibilityProfiles` column `headerEndRow` (Int); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| identityRowMarkers | Text | `CompatibilityProfiles` column `identityRowMarkers` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| columnOrderOnExport | Text | `CompatibilityProfiles` column `columnOrderOnExport` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| identityFieldOrder | Text | `CompatibilityProfiles` column `identityFieldOrder` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| knownUnsupported | Text | `CompatibilityProfiles` column `knownUnsupported` (Text); no adjacent `///` doc in schema file. | UNCLEAR | Compatibility/export mapping artifact; schema does not state per-field provenance (captured vs computed). | Medium | ARM export, compatibility builder |
| exportConfidence | Text | `CompatibilityProfiles` column `exportConfidence` (Text); no adjacent `///` doc in schema file. | FACT SNAPSHOT | Quality score evaluated at CSV parse time against data discarded after import. ArmColumnClassification list is not stored; raw CSV is discarded. Active hard gate on all four export paths. | Medium | Export gating, ARM workflows |
| exportBlockReason | Text | `CompatibilityProfiles` column `exportBlockReason` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| roundTripValidated | Bool | `CompatibilityProfiles` column `roundTripValidated` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| roundTripValidatedAt | DateTime | `CompatibilityProfiles` column `roundTripValidatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `CompatibilityProfiles` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| lastValidatedAt | DateTime | `CompatibilityProfiles` column `lastValidatedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `CropDescriptions`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `CropDescriptions` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `CropDescriptions` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plantingDate | DateTime | `CropDescriptions` column `plantingDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| transplantingDate | DateTime | `CropDescriptions` column `transplantingDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| emergenceDate | DateTime | `CropDescriptions` column `emergenceDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestDate | DateTime | `CropDescriptions` column `harvestDate` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| varietyOrHybrid | Text | `CropDescriptions` column `varietyOrHybrid` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedLot | Text | `CropDescriptions` column `seedLot` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| seedbedPreparation | Text | `CropDescriptions` column `seedbedPreparation` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| tillageType | Text | `CropDescriptions` column `tillageType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| standardMoisture | Real | `CropDescriptions` column `standardMoisture` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| moistureAtHarvest | Real | `CropDescriptions` column `moistureAtHarvest` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `CropDescriptions` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdBy | Text | `CropDescriptions` column `createdBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `TrialContacts`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `TrialContacts` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `TrialContacts` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialDirector | Text | `TrialContacts` column `trialDirector` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cooperator | Text | `TrialContacts` column `cooperator` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| sponsor | Text | `TrialContacts` column `sponsor` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| applicator | Text | `TrialContacts` column `applicator` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| assessor | Text | `TrialContacts` column `assessor` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `TrialContacts` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdBy | Text | `TrialContacts` column `createdBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `YieldDetails`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `YieldDetails` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `YieldDetails` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| plotId | Int | `YieldDetails` column `plotId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialAssessmentId | Int | `YieldDetails` column `trialAssessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestWeight | Real | `YieldDetails` column `harvestWeight` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestMoisture | Real | `YieldDetails` column `harvestMoisture` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| harvestedArea | Real | `YieldDetails` column `harvestedArea` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| convertedYield | Real | `YieldDetails` column `convertedYield` (Real); no adjacent `///` doc in schema file. | UNCLEAR | May be entered or formula-derived from weight and moisture. | Medium | Yield features |
| standardMoistureUsed | Real | `YieldDetails` column `standardMoistureUsed` (Real); no adjacent `///` doc in schema file. | UNCLEAR | May mirror protocol or be chosen by calculation path. | Low | Yield normalization |
| createdAt | DateTime | `YieldDetails` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdBy | Text | `YieldDetails` column `createdBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `TrialExportDiagnostics`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| trialId | Int | `TrialExportDiagnostics` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| publishedAt | DateTime | `TrialExportDiagnostics` column `publishedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| attemptLabel | Text | `TrialExportDiagnostics` column `attemptLabel` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| findingsJson | Text | `TrialExportDiagnostics` column `findingsJson` (Text); no adjacent `///` doc in schema file. | FACT SNAPSHOT | Record of findings from one specific past export attempt. Not current trial state; UI must label as "from last export attempt". | Medium | Export preflight, readiness UX |
| payloadVersion | Int | `TrialExportDiagnostics` column `payloadVersion` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `WeatherSnapshots`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `WeatherSnapshots` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| uuid | Text | `WeatherSnapshots` column `uuid` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| trialId | Int | `WeatherSnapshots` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| parentType | Text | `WeatherSnapshots` column `parentType` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| parentId | Int | `WeatherSnapshots` column `parentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| source | Text | `WeatherSnapshots` column `source` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| temperature | Real | `WeatherSnapshots` column `temperature` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| temperatureUnit | Text | `WeatherSnapshots` column `temperatureUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| humidity | Real | `WeatherSnapshots` column `humidity` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windSpeed | Real | `WeatherSnapshots` column `windSpeed` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windSpeedUnit | Text | `WeatherSnapshots` column `windSpeedUnit` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| windDirection | Text | `WeatherSnapshots` column `windDirection` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| cloudCover | Text | `WeatherSnapshots` column `cloudCover` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| precipitation | Text | `WeatherSnapshots` column `precipitation` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilCondition | Text | `WeatherSnapshots` column `soilCondition` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| notes | Text | `WeatherSnapshots` column `notes` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| recordedAt | Int | UTC epoch milliseconds when conditions were observed. (`recordedAt`, Int). | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | Int | `WeatherSnapshots` column `createdAt` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| modifiedAt | Int | `WeatherSnapshots` column `modifiedAt` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdBy | Text | `WeatherSnapshots` column `createdBy` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `Sites`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `Sites` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| name | Text | `Sites` column `name` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| latitude | Real | `Sites` column `latitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| longitude | Real | `Sites` column `longitude` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| elevationM | Real | `Sites` column `elevationM` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilSeries | Text | `Sites` column `soilSeries` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilTexture | Text | `Sites` column `soilTexture` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| soilPh | Real | `Sites` column `soilPh` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| organicMatterPct | Real | `Sites` column `organicMatterPct` (Real); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| previousCrop | Text | `Sites` column `previousCrop` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| tillage | Text | `Sites` column `tillage` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| irrigated | Bool | `Sites` column `irrigated` (Bool); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| fieldName | Text | `Sites` column `fieldName` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| county | Text | `Sites` column `county` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| stateProvince | Text | `Sites` column `stateProvince` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| country | Text | `Sites` column `country` (Text); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| createdAt | DateTime | `Sites` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |
| modifiedAt | DateTime | `Sites` column `modifiedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Treated as captured or user/import/session-attributed data unless noted; schema alone does not prove it is only a computation cache. | Low | Features referencing this entity (see domain code) |

### `ArmColumnMappings`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ArmColumnMappings` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Bridge row keys linking ARM column identity to app assessment/session. | Medium | ARM import/export |
| trialId | Int | `ArmColumnMappings` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Bridge row keys linking ARM column identity to app assessment/session. | Medium | ARM import/export |
| armColumnId | Text | ARM Column ID exactly as it appears in the shell (typically numeric but kept as text to preserve leading zeros or future non-numeric IDs). (`armColumnId`, Text). | ROUND-TRIP STORAGE | ARM Column ID string as in shell. | Medium | ARM column bridge |
| armColumnIdInteger | Int | Integer form of [armColumnId] when parseable; null otherwise. Used as the canonical round-trip matching key where the shell offers it. (`armColumnIdInteger`, Int). | ROUND-TRIP STORAGE | Numeric parse of ARM column id for shell matching; mirrors textual id for export. | Low | ARM column bridge, export |
| armColumnIndex | Int | 0-based position of the column within the shell's assessment-column area. Stable across import/export as long as the shell is not reshaped. (`armColumnIndex`, Int). | ROUND-TRIP STORAGE | 0-based shell column index for ordering. | Medium | ARM export column order |
| trialAssessmentId | Int | The deduplicated app-side assessment this column belongs to. Null = orphan column (preserved for round-trip, hidden from UI). (`trialAssessmentId`, Int). | RAW FACT | Bridge row keys linking ARM column identity to app assessment/session. | Medium | ARM import/export |
| sessionId | Int | The app-side session this column's rating date maps to. Null = orphan column (see above). (`sessionId`, Int). | RAW FACT | Bridge row keys linking ARM column identity to app assessment/session. | Medium | ARM import/export |
| createdAt | DateTime | `ArmColumnMappings` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Bridge row keys linking ARM column identity to app assessment/session. | Medium | ARM import/export |

### `ArmAssessmentMetadata`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ArmAssessmentMetadata` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Identity / FK / audit for per-assessment ARM metadata row. | Low | ARM protocol, import, export |
| trialAssessmentId | Int | `ArmAssessmentMetadata` column `trialAssessmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Identity / FK / audit for per-assessment ARM metadata row. | Low | ARM protocol, import, export |
| seName | Text | ARM "SE Name" code (e.g. "W003", "CF013"). Part of the dedup identity. (`seName`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| seDescription | Text | Verbatim "SE Description" from the shell (e.g. "Weed Control"). (`seDescription`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| partRated | Text | ARM "Part Rated" (e.g. "PLANT", "LEAF3"). Part of the dedup identity. (`partRated`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| ratingType | Text | ARM "Rating Type" (e.g. "CONTRO", "LODGIN", "PESINC"). Part of the dedup identity. (`ratingType`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| ratingUnit | Text | `ArmAssessmentMetadata` column `ratingUnit` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| ratingMin | Real | `ArmAssessmentMetadata` column `ratingMin` (Real); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| ratingMax | Real | `ArmAssessmentMetadata` column `ratingMax` (Real); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| collectBasis | Text | "P" = per plot, "S" = per subsample. (`collectBasis`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| numSubsamples | Int | Number of subsamples per plot when [collectBasis] = "S". (`numSubsamples`, Int). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| pestCode | Text | Primary pest / weed / disease target code (EPPO or ARM code). (`pestCode`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| pestCodeSecondary | Text | Optional secondary target code when the assessment covers two. (`pestCodeSecondary`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| armImportColumnIndex | Int | Original CSV column index (0-based) of the assessment column in the source Plot Data file. Used to preserve export ordering round-trip. (`armImportColumnIndex`, Int). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| armShellColumnId | Text | ARM shell Column ID (row 7) as a raw string (e.g. "0001"). Preserved verbatim so round-trip export can emit the exact cell ARM provided. (`armShellColumnId`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| armColumnIdInteger | Int | Integer form of the ARM Column ID (row 7, parsed). Primary export anchor when we need to key by number rather than the raw string. (`armColumnIdInteger`, Int). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| armShellRatingDate | Text | Shell rating-date cell (row 15) as the raw display string, including any trailing markers; paired with [ArmSessionMetadata.armRatingDate]. (`armShellRatingDate`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestType | Text | `ArmAssessmentMetadata` column `shellPestType` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestName | Text | `ArmAssessmentMetadata` column `shellPestName` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropCode | Text | `ArmAssessmentMetadata` column `shellCropCode` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropName | Text | `ArmAssessmentMetadata` column `shellCropName` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropVariety | Text | `ArmAssessmentMetadata` column `shellCropVariety` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellRatingTime | Text | `ArmAssessmentMetadata` column `shellRatingTime` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropOrPest | Text | `ArmAssessmentMetadata` column `shellCropOrPest` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellSampleSize | Text | `ArmAssessmentMetadata` column `shellSampleSize` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellSizeUnit | Text | `ArmAssessmentMetadata` column `shellSizeUnit` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCollectionBasisUnit | Text | `ArmAssessmentMetadata` column `shellCollectionBasisUnit` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellReportingBasis | Text | `ArmAssessmentMetadata` column `shellReportingBasis` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellReportingBasisUnit | Text | `ArmAssessmentMetadata` column `shellReportingBasisUnit` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellStageScale | Text | `ArmAssessmentMetadata` column `shellStageScale` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropStageMaj | Text | `ArmAssessmentMetadata` column `shellCropStageMaj` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropStageMin | Text | `ArmAssessmentMetadata` column `shellCropStageMin` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropStageMax | Text | `ArmAssessmentMetadata` column `shellCropStageMax` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropDensity | Text | `ArmAssessmentMetadata` column `shellCropDensity` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellCropDensityUnit | Text | `ArmAssessmentMetadata` column `shellCropDensityUnit` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestStageMaj | Text | `ArmAssessmentMetadata` column `shellPestStageMaj` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestStageMin | Text | `ArmAssessmentMetadata` column `shellPestStageMin` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestStageMax | Text | `ArmAssessmentMetadata` column `shellPestStageMax` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestDensity | Text | `ArmAssessmentMetadata` column `shellPestDensity` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPestDensityUnit | Text | `ArmAssessmentMetadata` column `shellPestDensityUnit` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellAssessedBy | Text | `ArmAssessmentMetadata` column `shellAssessedBy` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellEquipment | Text | `ArmAssessmentMetadata` column `shellEquipment` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellUntreatedRatingType | Text | `ArmAssessmentMetadata` column `shellUntreatedRatingType` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellArmActions | Text | `ArmAssessmentMetadata` column `shellArmActions` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellAppTimingCode | Text | Row 41 (0-based) — ARM `035EET` Rating / App timing code (A1, A3, AA). (`shellAppTimingCode`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellTrtEvalInterval | Text | Row 42 (0-based) — ARM `036ETI` treatment-evaluation interval. (`shellTrtEvalInterval`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| shellPlantEvalInterval | Text | Row 43 (0-based) — ARM `037EPI` plant-evaluation / DAT interval. (`shellPlantEvalInterval`, Text). | ROUND-TRIP STORAGE | Shell-sourced assessment metadata kept verbatim for Rating Shell round-trip. | Medium | ARM import/export, protocol tab |
| createdAt | DateTime | `ArmAssessmentMetadata` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Identity / FK / audit for per-assessment ARM metadata row. | Low | ARM protocol, import, export |

### `ArmSessionMetadata`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ArmSessionMetadata` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Identity / FK / audit for per-session ARM metadata. | Low | Sessions, ARM mapping |
| sessionId | Int | `ArmSessionMetadata` column `sessionId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Identity / FK / audit for per-session ARM metadata. | Low | Sessions, ARM mapping |
| armRatingDate | Text | Rating Date from the ARM shell, stored as a YYYY-MM-DD string to match the shell's own representation and the app's [Sessions.sessionDateLocal]. (`armRatingDate`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| timingCode | Text | ARM timing code (e.g. "A1", "A3", "A6", "AA") identifying the pre/post-application slot this rating belongs to. (`timingCode`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| cropStageMaj | Text | Major crop stage (ARM "Crop Stage Maj", e.g. "V5", "R1", "BBCH 30"). (`cropStageMaj`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| cropStageMin | Text | Minor crop stage (ARM "Crop Stage Min"). (`cropStageMin`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| cropStageScale | Text | Scale used for the crop stage (e.g. "BBCH", "Feekes"). (`cropStageScale`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| trtEvalInterval | Text | Days-after-treatment interval string (e.g. "21 DA-A"). (`trtEvalInterval`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| plantEvalInterval | Text | Days-after-planting interval string (e.g. "14 DA-P"). (`plantEvalInterval`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| raterInitials | Text | Rater initials from the Plot Data header; may differ per rating date. (`raterInitials`, Text). | ROUND-TRIP STORAGE | Shell date/timing/stage strings for export fidelity. | Medium | ARM export |
| createdAt | DateTime | `ArmSessionMetadata` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Identity / FK / audit for per-session ARM metadata. | Low | Sessions, ARM mapping |

### `ArmTrialMetadata`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| trialId | Int | `ArmTrialMetadata` column `trialId` (Int); no adjacent `///` doc in schema file. | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| isArmLinked | Bool | `ArmTrialMetadata` column `isArmLinked` (Bool); no adjacent `///` doc in schema file. | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| armImportedAt | DateTime | `ArmTrialMetadata` column `armImportedAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| armSourceFile | Text | `ArmTrialMetadata` column `armSourceFile` (Text); no adjacent `///` doc in schema file. | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| armVersion | Text | `ArmTrialMetadata` column `armVersion` (Text); no adjacent `///` doc in schema file. | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| armImportSessionId | Int | Session used for ARM import ratings; preferred for Rating Shell export. Plain int (no FK) to avoid Drift circular ref: sessions already reference trials. (`armImportSessionId`, Int). | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| armLinkedShellPath | Text | Last ARM Rating Shell (.xlsx) path applied from the shell link workflow. (`armLinkedShellPath`, Text). | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| armLinkedShellAt | DateTime | When [armLinkedShellPath] was last applied. (`armLinkedShellAt`, DateTime). | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| shellInternalPath | Text | Internal app path where the shell file is stored (copied at import/link). (`shellInternalPath`, Text). | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |
| shellCommentsSheet | Text | Free-text from the shell **Comments** sheet (`ECM` row, column B). (`shellCommentsSheet`, Text). | RAW FACT | ARM linkage/import artifact for this trial (paths, timestamps, session pointers as recorded). | Medium | ARM shell workflow, export session pick |

### `ArmTreatmentMetadata`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ArmTreatmentMetadata` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Identity / link to core treatment / audit. | Low | ARM protocol tab |
| treatmentId | Int | `ArmTreatmentMetadata` column `treatmentId` (Int); no adjacent `///` doc in schema file. | RAW FACT | Identity / link to core treatment / audit. | Low | ARM protocol tab |
| armTypeCode | Text | ARM "Type" column (e.g. "H" = herbicide, "F" = fungicide). Free text because ARM's list evolves; the mapping to a core concept (if any) is a display concern. (`armTypeCode`, Text). | ROUND-TRIP STORAGE | ARM Treatments sheet values for round-trip. | Medium | ARM import/export |
| formConc | Real | ARM "Form Conc" — formulation concentration as a real number (e.g. 480). Paired with [formConcUnit]. (`formConc`, Real). | ROUND-TRIP STORAGE | ARM Treatments sheet values for round-trip. | Medium | ARM import/export |
| formConcUnit | Text | ARM "Form Conc Unit" — formulation concentration unit string (e.g. "%W/W", "%W/V", "G/L"). Stored verbatim so round-trip export emits exactly what ARM provided. (`formConcUnit`, Text). | ROUND-TRIP STORAGE | ARM Treatments sheet values for round-trip. | Medium | ARM import/export |
| formType | Text | ARM "Form Type" — formulation type code (e.g. "SC", "EC", "WG"). (`formType`, Text). | ROUND-TRIP STORAGE | ARM Treatments sheet values for round-trip. | Medium | ARM import/export |
| armRowSortOrder | Int | 0-based row position of the treatment in the ARM Treatments sheet. Preserves the importer's original ordering for export. (`armRowSortOrder`, Int). | ROUND-TRIP STORAGE | ARM Treatments sheet values for round-trip. | Medium | ARM import/export |
| createdAt | DateTime | `ArmTreatmentMetadata` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Identity / link to core treatment / audit. | Low | ARM protocol tab |

### `ArmApplications`

| Column | Type | Current Purpose | Classification | Why | Migration Risk | Screens or Features Likely Depending On It |
| --- | --- | --- | --- | --- | --- | --- |
| id | Int | `ArmApplications` column `id` (Int); no adjacent `///` doc in schema file. | RAW FACT | Primary key for ARM Applications sheet mirror row. | Low | ARM shell import/export |
| trialApplicationEventId | Text | `ArmApplications` column `trialApplicationEventId` (Text); no adjacent `///` doc in schema file. | RAW FACT | FK linking shell block to `TrialApplicationEvents` row. | Medium | Canonical applications + ARM export |
| armSheetColumnIndex | Int | 0-based worksheet column index of this application block (Excel `C` → 2). (`armSheetColumnIndex`, Int). | ROUND-TRIP STORAGE | Worksheet column index for this application block in the shell. | Medium | ARM export layout |
| row01 | Text | `ArmApplications` column `row01` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row02 | Text | `ArmApplications` column `row02` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row03 | Text | `ArmApplications` column `row03` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row04 | Text | `ArmApplications` column `row04` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row05 | Text | `ArmApplications` column `row05` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row06 | Text | `ArmApplications` column `row06` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row07 | Text | `ArmApplications` column `row07` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row08 | Text | `ArmApplications` column `row08` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row09 | Text | `ArmApplications` column `row09` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row10 | Text | `ArmApplications` column `row10` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row11 | Text | `ArmApplications` column `row11` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row12 | Text | `ArmApplications` column `row12` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row13 | Text | `ArmApplications` column `row13` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row14 | Text | `ArmApplications` column `row14` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row15 | Text | `ArmApplications` column `row15` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row16 | Text | `ArmApplications` column `row16` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row17 | Text | `ArmApplications` column `row17` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row18 | Text | `ArmApplications` column `row18` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row19 | Text | `ArmApplications` column `row19` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row20 | Text | `ArmApplications` column `row20` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row21 | Text | `ArmApplications` column `row21` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row22 | Text | `ArmApplications` column `row22` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row23 | Text | `ArmApplications` column `row23` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row24 | Text | `ArmApplications` column `row24` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row25 | Text | `ArmApplications` column `row25` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row26 | Text | `ArmApplications` column `row26` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row27 | Text | `ArmApplications` column `row27` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row28 | Text | `ArmApplications` column `row28` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row29 | Text | `ArmApplications` column `row29` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row30 | Text | `ArmApplications` column `row30` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row31 | Text | `ArmApplications` column `row31` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row32 | Text | `ArmApplications` column `row32` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row33 | Text | `ArmApplications` column `row33` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row34 | Text | `ArmApplications` column `row34` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row35 | Text | `ArmApplications` column `row35` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row36 | Text | `ArmApplications` column `row36` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row37 | Text | `ArmApplications` column `row37` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row38 | Text | `ArmApplications` column `row38` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row39 | Text | `ArmApplications` column `row39` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row40 | Text | `ArmApplications` column `row40` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row41 | Text | `ArmApplications` column `row41` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row42 | Text | `ArmApplications` column `row42` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row43 | Text | `ArmApplications` column `row43` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row44 | Text | `ArmApplications` column `row44` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row45 | Text | `ArmApplications` column `row45` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row46 | Text | `ArmApplications` column `row46` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row47 | Text | `ArmApplications` column `row47` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row48 | Text | `ArmApplications` column `row48` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row49 | Text | `ArmApplications` column `row49` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row50 | Text | `ArmApplications` column `row50` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row51 | Text | `ArmApplications` column `row51` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row52 | Text | `ArmApplications` column `row52` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row53 | Text | `ArmApplications` column `row53` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row54 | Text | `ArmApplications` column `row54` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row55 | Text | `ArmApplications` column `row55` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row56 | Text | `ArmApplications` column `row56` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row57 | Text | `ArmApplications` column `row57` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row58 | Text | `ArmApplications` column `row58` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row59 | Text | `ArmApplications` column `row59` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row60 | Text | `ArmApplications` column `row60` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row61 | Text | `ArmApplications` column `row61` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row62 | Text | `ArmApplications` column `row62` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row63 | Text | `ArmApplications` column `row63` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row64 | Text | `ArmApplications` column `row64` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row65 | Text | `ArmApplications` column `row65` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row66 | Text | `ArmApplications` column `row66` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row67 | Text | `ArmApplications` column `row67` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row68 | Text | `ArmApplications` column `row68` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row69 | Text | `ArmApplications` column `row69` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row70 | Text | `ArmApplications` column `row70` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row71 | Text | `ArmApplications` column `row71` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row72 | Text | `ArmApplications` column `row72` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row73 | Text | `ArmApplications` column `row73` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row74 | Text | `ArmApplications` column `row74` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row75 | Text | `ArmApplications` column `row75` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row76 | Text | `ArmApplications` column `row76` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row77 | Text | `ArmApplications` column `row77` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row78 | Text | `ArmApplications` column `row78` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| row79 | Text | `ArmApplications` column `row79` (Text); no adjacent `///` doc in schema file. | ROUND-TRIP STORAGE | Verbatim Applications-sheet cell text for ARM round-trip (rows 01–79). | High | ARM shell export |
| createdAt | DateTime | `ArmApplications` column `createdAt` (DateTime); no adjacent `///` doc in schema file. | RAW FACT | Row insert timestamp. | Low | ORM / diagnostics |

## Section A — Valid stored facts that sound like derived state

| Column | Table | Explanation |
| --- | --- | --- |
| `status` | `Trials`, `Sessions`, `ApplicationEvents`, `ApplicationPlotRecords`, `SeedingEvents`, `TrialApplicationEvents`, `Photos` | Stored workflow or entity state as explicitly set or imported, not an automatic aggregate. |
| `excludeFromAnalysis` | `Plots` | Operator/analyst decision to exclude a plot, recorded as a fact at decision time. |
| `isCheck` / `isControl` | `Assignments` | Protocol roles for plots/treatments as captured in the trial design/import. |
| `isGuardRow` | `Plots` | Declared non-data/border plot for layout. |
| `partialFlag` | `ApplicationEvents` | Observation that coverage was partial. |
| `roundTripValidated` | `CompatibilityProfiles` | Records that a validation outcome occurred, not the full recomputation. |
| `ratingValue` | `Photos` | Numeric rating associated at photo capture time. |
| `resultStatus` | `RatingRecords` | Entry/flow status for that rating observation. |
| `deviationNotes` | `TrialApplicationProducts` | Free-text explanation of deviation (the notes are factual utterances even if a flag beside them were computed). |

## Section B — Dangerous derived fields

_Note: `exportConfidence`, `findingsJson`, and `hasSparseData` were previously listed here. They are reclassified as FACT SNAPSHOT (see Architecture Rule Summary) and removed from this section. They are not migration targets._

| Column | Table | Why dangerous | Dependent areas |
| --- | --- | --- | --- |
| `isCurrent` | `RatingRecords` | Authoritative “current” flag can disagree with amendment chain if not maintained perfectly. Accepted exception — see TRIAL_MODEL.md. | Rating UI, history, exports. |
| `plotsTreated` | `TrialApplicationEvents` | Parallel representation to `ApplicationPlotAssignments`; risk of drift. Active migration target. | Applications UI, export. |
| `rowsImported` / `rowsSkipped` | `ImportEvents` | Aggregate-only view of import without per-row truth. | Import history, support. |
| `plotCount` / `treatmentCount` / `assessmentCount` / pattern flags | `ImportSnapshots` | Summaries of a parse; re-running importer may change meaning. | ARM import, profiles. |

## Section C — Highest-risk derived columns to migrate

_Note: `findingsJson` (former rank 3), `exportConfidence` (former rank 7), and `hasSparseData` (former rank 10) are reclassified as FACT SNAPSHOT and removed from this list. They are not migration targets._

| Rank | Table | Column | Reason |
| --- | --- | --- | --- |
| 1 | `RatingRecords` | `isCurrent` | Broad dependence; corruption affects every plot/assessment “current” view. Accepted exception — not a migration target, but highest maintenance risk. |
| 2 | `TrialApplicationEvents` | `plotsTreated` | Explicit denormalized cache; duplicates structured facts in ApplicationPlotAssignments. Active migration target. |
| 3 | `ImportSnapshots` | `plotCount` | Structural summary feeding downstream import/export decisions. |
| 4 | `ImportSnapshots` | `assessmentCount` | Same class of structural summary. |
| 5 | `ImportSnapshots` | `treatmentCount` | Same class of structural summary. |
| 6 | `ImportEvents` | `rowsImported` | Shown as import success metric. |
| 7 | `ImportEvents` | `rowsSkipped` | Paired metric; completeness narrative. |

## Section D — Architectural red flags

Tables with **>50%** of columns classified **DERIVED STATE**.

No table exceeds the 50% DERIVED STATE threshold. After FACT SNAPSHOT reclassification: `TrialExportDiagnostics` has no remaining DERIVED STATE columns (findingsJson reclassified). `ImportSnapshots` DERIVED STATE percentage reduced (hasSparseData reclassified). `CompatibilityProfiles` still includes multiple **UNCLEAR** mapping columns pending provenance review; exportConfidence is reclassified as FACT SNAPSHOT.